import { useHistory } from "react-router-dom";

const HandleLogout = () => {

    const handleLogout = () => {
        console.log("in handlelogout")
        return (
            <div>hello</div>
            
        )
    };
};

export default HandleLogout;